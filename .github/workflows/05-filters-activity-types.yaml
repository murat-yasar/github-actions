name: 05 - Filters - Activity Types

on:
   pull_request:
      types:
         - opened
         - synchronize
      branches:
         - main

jobs:
   echo:
     runs-on: ubuntu-latest
     steps:
       - run: echo "Run whenever a PR is opened and synchronized AND base branch is main"